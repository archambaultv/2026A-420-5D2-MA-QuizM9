node_modules/
dist/
build/
.react-router/

# Les secrets : jamais dans le dépôt. Voir server/.env.example.
.env

# La base SQLite : celle des semaines 1 à 4, ou celle d'un poste sans Docker.
server/data/*.db
server/data/*.db-*
